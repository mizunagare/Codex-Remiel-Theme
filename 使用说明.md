# 蕾米埃尔｜星芒圣翼 Codex Dream Skin

这是为 Codex Desktop 制作的蕾米埃尔主题包：人物焦点位于右侧，左侧保留代码与对话阅读区；配色以星芒粉、机械银白和深紫为主，并通过 `appearance: auto` 适配浅色与深色外观。

## 安装

1. 双击 `安装主题工具.cmd`。
2. 如果 Codex 正在运行，安装器会提示确认重启，避免未经确认丢失输入。
3. 工具会复制到 `%LOCALAPPDATA%\CodexDreamSkin\runtime`，并创建桌面 `Codex 主题` 入口。
4. 首次启动只出现 `设置 → 主题` 管理页，并保持 Codex 官方外观。
5. 在主题页的“可安装主题”中点击“安装主题”，再从“已安装主题”启用蕾米埃尔。
6. 以后拿到新版安装包，重新双击即可原位升级；Store 更新导致注入器失效时，后台守护会自动匹配最新的已注册 Codex 包并修复。

`安装蕾米埃尔主题.cmd` 是兼容入口；建议安装主题管理工具，再在主题页完成主题安装。

也可以在 PowerShell 中运行：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\install-remiel.ps1
```

在设置的“主题”页中可以选择主题宠物。主题清单当前绑定 `remiel-switch`，标准 v2 宠物包位于 `windows/pets/remiel-switch/`；通过 GitHub 或本地主题库换机后，启用主题会自动安装并选中它，同时保留用户已有的其他宠物。

## 主题页

打开 Codex 的 `设置 → 主题`。这是独立渲染的主题管理页，只依赖设置导航的语义标记，不嵌入“外观”等原生设置内容，因此常规版本更新不需要跟着内部布局逐项调整。

在这里可以：

- 切换已安装主题；
- 安装安装包内置主题；
- 保存主题文件后自动热重载，不重启 Codex；
- 一键“还原官方外观”，主题和主题库仍会保留；
- 再次启用当前主题；
- 添加本地主题文件夹；
- 直接添加公开的 GitHub 主题仓库；
- 添加 HTTPS 远程主题库索引并安装其中的主题。
- 读取 `~/.codex/pets` 中的原生 v2 宠物，并把所选宠物作为独立 `pets/` 包绑定到主题。

每个主题现在都是独立代码包，目录结构为：

```text
主题文件夹/
├─ theme.json
├─ theme.css
├─ theme.js
├─ background.jpg
windows/pets/<pet-id>/   # 可选：随主题同步的独立 v2 宠物包
├─ pet.json
└─ spritesheet.webp
```

图标、动画和页面适配应写在该主题自己的 `theme.css/theme.js` 中，不再放进公共注入器。完整格式见 `windows/THEME_FORMAT.md`。

远程索引的最小格式：

```json
{ "themes": [{ "id": "theme-id", "name": "主题名", "themeUrl": "https://example.com/theme.json" }] }
```

GitHub 仓库安装使用仓库根目录的 `theme-library.json`。主题页只接受公开 HTTPS GitHub 仓库；私有仓库不会要求或保存 GitHub Token。

## 热重载

注入器会监听当前活动主题的 `theme.json`、`theme.css`、`theme.js` 和图片文件。文件保存并稳定约 120 ms 后会重新校验和注入；无效修改会被拒绝，当前可用主题继续保留。文件系统监听不可用时自动退回定时校验。

## 主题宠物

主题页只读取并验证 `~/.codex/pets/<id>/pet.json` 和 `spritesheet.webp`，仅显示标准 v2 有效包。选择后会复制到主题的 `pets/<id>/`；换机安装并启用主题时，会同步安装和选择该宠物。宠物显示和动画仍由 Codex 原生宠物窗口负责。

## 还原与卸载

设置页的“还原官方外观”会暂停视觉主题并恢复安装前的基础外观配置，但不删除主题、宠物和主题库。

桌面的 `Codex Dream Skin - Restore` 用于完整恢复：关闭主题会话并恢复基础配置。安装器会把原配置备份到 `%LOCALAPPDATA%\CodexDreamSkin`。

## 安全边界

- 仅使用 `127.0.0.1` 上的本机 CDP 端口。
- 不修改 `WindowsApps`、`app.asar` 或 Codex 安装文件。
- 自动修复只识别已注册且签名类型为 Store 的 `OpenAI.Codex` 包，并继续只使用回环 CDP。
- 壁纸是本主题使用的同人视觉，不随工具修改 Codex 官方资源。

底层注入方案来自 Fei-Away/Codex-Dream-Skin；原仓库说明保留在同目录的 `README.md`。
