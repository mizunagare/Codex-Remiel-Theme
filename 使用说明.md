# 蕾米埃尔｜星芒圣翼 Codex Theme

这是为 Codex Desktop 制作的自包含 v2 主题包：蕾米埃尔位于右侧，左侧保留代码、项目和对话阅读区；配色以星芒粉、机械紫、珍珠白和深梅黑为主，并同时适配 Codex 的浅色与深色外观。

## 推荐安装

1. 完全关闭 Codex。
2. 双击仓库根目录的 `安装主题工具.cmd`。
3. 安装完成后，通过桌面 `Codex 主题` 打开 Codex。
4. 进入 `设置 → 主题`。
5. 在“可安装主题”中安装“蕾米埃尔｜星芒圣翼”，随后在“已安装主题”中启用。

首次启用会询问是否重启正在运行的 Codex，不会在后台静默重启。主题不会修改 `WindowsApps`、`app.asar` 或 Codex 官方文件。

## 兼容安装入口

也可以双击 `安装蕾米埃尔主题.cmd`，或在 PowerShell 中运行：

```powershell
powershell -NoProfile -ExecutionPolicy RemoteSigned -File .\install-remiel.ps1
```

## 主题特点

- 16:9 连续全窗背景，首页突出角色，任务页降低图片干扰；
- `appearance: auto` 跟随 Codex 当前浅色或深色外观；
- 侧栏、项目选择器、建议卡片和输入区保持原生交互；
- 新建任务、搜索、项目、插件、权限、听写、发送等按钮使用统一星芒圣翼图标；
- 发送与加载状态仅使用 CSS transform 动画，不使用逐帧滤镜或大面积实时模糊；
- 不捆绑宠物，不修改 `~/.codex/pets` 和当前宠物选择。

## GitHub 安装

主题发布到公开 GitHub 仓库后，可在 `设置 → 主题 → 从 GitHub 仓库安装` 中粘贴仓库 HTTPS 地址。主题管理器读取根目录的 `theme-library.json`，下载并校验完整主题包后再安装。

## 恢复官方外观

进入 `设置 → 主题`，选择“还原官方外观”；也可以使用安装器创建的恢复入口。恢复操作会移除实时主题并关闭本地 CDP 会话，不删除任务、插件、宠物或登录状态。

## 验证

```powershell
powershell -NoProfile -File .\windows\tests\run-tests.ps1
node --check .\windows\scripts\injector.mjs
node --check .\windows\themes\remiel-seraph-system-v1\theme.js
```

背景必须保持无 UI、无侧栏、无输入框、无文字和无水印，且不超过 16 MB、16384 px 单边或 50 MP。
